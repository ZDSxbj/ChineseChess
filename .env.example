# 将此文件复制为 .env 并按需修改
# 生产环境前端地址（用于后端 CORS 配置，支持逗号分隔多个）
# 如果前端与后端同域部署（推荐），可以写为你的域名，例如：
FRONTEND_URL=https://your-domain.com

# 后端在 Nginx 下的公开前缀，前端默认以 /chess 为 base
# 因此这里建议使用相对路径，避免写死域名
PUBLIC_API_PREFIX=/chess/api

# 如需为 Redis 设置密码，请取消注释并填入
# REDIS_PASSWORD=
