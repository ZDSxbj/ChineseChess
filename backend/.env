# MySQL配置（本地数据库）
DB_HOST=localhost       # 本地MySQL地址
DB_PORT=3306            # 本地MySQL端口（默认3306）
DB_USERNAME=root        # 本地MySQL用户名（通常为root）
DB_PASSWORD=Fndjjfly520@  # 本地MySQL密码（若未设置密码则留空）
DB_NAME=chinese_chess   # 数据库名（需提前创建）
DB_LOG_LEVEL=info       # 日志级别（可选：silent/error/warn/info）

# Redis配置（本地Redis）
REDIS_HOST=localhost    # 本地Redis地址
REDIS_PORT=6379         # 本地Redis端口（默认6379）
REDIS_PASSWORD=
# 前端地址（用于跨域配置）
FRONTEND_URL=http://localhost:5173  # 假设前端本地启动端口为5173

# 运行环境（非docker模式，会加载.env文件）
GO_ENV=local
GIN_MODE=debug  # 开发模式（可选，便于调试）